#+TITLE: test html form
# TAGS #html

How to take a look at what an multipart post sends:

nc -l localhost 8000

open a file in the web browser containing this:

#+begin_src html
  <!DOCTYPE html>
  <html lang="en">
    <head>
      <meta charset="utf-8"/>
      <title>upload</title>
    </head>
    <body>
      <form action="http://localhost:8000" method="post" enctype="multipart/form-data">
        <p><input type="text" name="text1" value="text default">
        <p><input type="text" name="text2" value="a&#x03C9;b">
        <p><input type="file" name="file1">
        <p><input type="file" name="file2">
        <p><input type="file" name="file3">
        <p><button type="submit">Submit</button>
      </form>
    </body>
  </html>
#+end_src

use these commands to create different file tyes:

echo 'Content of a.txt.' > a.txt

echo '<!DOCTYPE html><title>Content of a.html.</title>' > a.html

# Binary file containing 4 bytes: 'a', 1, 2 and 'b'.
printf 'a\xCF\x89b' > binary

select these files on the web page displayed and click send. Output should appear in the console.

from:https://stackoverflow.com/a/28380690/1671119
and: https://stackoverflow.com/a/23518189/1671119
